<div>
  <%= image_tag("bg.png", width: '100%', height: '300px') %>
</div>
  <div class="container-fluid text-center">
    <h1>Locations populaires</h1>
  </div>
    <div class="container">
       <div class="row">
        <% @cars.each do |car| %>
          <div class="col-4">
            <div class="home-card">
              <div class="col-6">
                <p> <%= car.price_per_day %>€ par jour </p>
                <p><%= car.brand %> <%= car.model %></p>
                  <div class="home-button text-center mt-3">
                   <%= link_to "Voir l'offre", car_path(car) %>
                   </div>
              </div>
                <div class="col-6">
                  <div class="home-card-name text-center">
                  <p> <%= car.user.first_name %> <%= car.user.last_name %> </p>
                  <%= cl_image_tag(car.photo.key, height: 100, crop: :fill) %>
                  </div>
                </div>
          </div>
        </div>
            <% end %>
       </div>
    </div>
